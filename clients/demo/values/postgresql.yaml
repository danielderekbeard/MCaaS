global:
  postgresql:
    auth:
      existingSecret: "demo-postgresql-secret"
      secretKeys:
        postgresPasswordKey: "postgres-password"
database: "demo_db"
primary:
  persistence:
    enabled: true
    size: 10Gi